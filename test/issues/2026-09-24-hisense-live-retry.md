# 海信 1.16.12 实页重试

日期：2026-09-24。范围：已有登录的 Google Chrome for Testing，海信简历编辑页 `https://jobs.hisense.com/form?fromPage=editMyResume`。固定模拟资料、AI 关闭、保留已有手机号，不保存或提交。用户补充：无法可靠确定的字段可以留空，不为填满耗时反复尝试。

## 结果

| 运行 | 版本 / runId | 耗时 | 核验 / 页面状态 |
| --- | --- | --- | --- |
| 前轮结果补取 | 1.16.10 / `ee3fa32d-4850-4115-9e8e-19a1dc2714f4` | 58.548 秒 | 101 项终验、102 非空、4 空白。此前因锁屏未读到，本轮已导出确认 |
| 新页面整页填写 | 1.16.12 / `6e5a7d43-20c3-4557-ba5b-ff94d76c7809` | 62.928 秒 | 101 项终验、102 非空、4 空白；原有手机号保留。分析 5.9 秒、填写 51.4 秒、核验 5.6 秒 |
| 同页仅补空白 | 1.16.12 / `8b9561cd-2e9e-47ce-8285-7909bfcb4843` | 15.818 秒 | 本轮新增 1 项终验、103 非空、3 空白。分析 4.6 秒、填写 3.2 秒、核验 8.0 秒；不覆盖已有内容 |

**15.818 秒不是整页从空白填满的时间。** 海信本轮整页 62.928 秒，尚未达到十几秒目标；相比前轮的单次结果也没有提速。1.16.12 在 UD 夹具上的优化不能直接外推到 Phoenix 实页。

当前 103 非空 = 第一轮本轮核验的 101 项 + 明确来源补填 1 项 + 原有手机号 1 项。补填运行只对新增的来源核验为通过，其余按已有值保留；不能把 103 非空写成同一轮 103 项终验。侧栏的“需核对”还包含已有字段及本页未提供的资料字段，不能等同于页面空白数。

教育 3 条，语言、项目、实习、获奖各 2 条，研究成果 1 条，共 12 条；补填前后各类条数不变，没有重复新增。学校联想层和三级籍贯不再阻塞后续下拉、日期、单选；首轮这些字段均进入终验。仍不代表海信所有入口和全部北森租户已支持。

## 剩余项及处理

- 招聘来源：旧演示值“校园就业网”未匹配到选项；实页提供“学校就业网/就业公众号”。仅在插件管理页将演示资料改为该完整选项，再由插件补填；日志 `recruitmentSource: stable-match`，返回海信页面后可见值一致。没有手动点选来冒充插件成功。
- 证件号：全零前缀的无效模拟身份证被网站回退。已从本浏览器模拟资料中清空，补填时按缺资料跳过，没有反复重试或生成有效身份号码。
- 高中学科、学位：演示高中记录未提供明确值。已只读查看候选；学科列表是大学学科门类，学位虽有“无”，本轮沿用用户新要求，两项均留空，没有猜选或修改原记录。
- 证件照、简历及附件：未上传。上传槽位不包含在 106 个普通控件的计数中，不能被非空率掩盖。
- 网站保存、刷新恢复、正式投递：未执行、未验证。

## 证据与版本

扩展管理页确认从桌面项目加载，重新加载后显示 `1.16.12-dev`；新页面 `2051929096` 的侧栏版本和导出日志 `contentBuild` 同为 `1.16.12-dev`。旧页面保留，没有刷新清空已有表单。

原始导出仅存放于忽略目录，不推送：

- `artifacts/hisense-live/qiuzhao-run-logs-2026-09-24T00-36-33-911Z.json`：前轮结果。
- `artifacts/hisense-live/qiuzhao-run-logs-2026-09-24T00-46-18-609Z.json`：本轮两次运行与历史记录。

基础演示资料为 `test/fixtures/xpeng-hisense-demo-profile.json`；本轮补填仅在浏览器资料中将 `recruitmentSource` 设为网站完整选项、`idNumber` 清空，其他字段保持。历史夹具仍保留原始无效证件号，便于复现拒绝行为。

本轮没有修改运行时代码，不新增版本。实际验证为生产扩展的两次海信实页操作、日志计数与目标字段回读；沿用 1.16.12 已通过的 49 脚本、21 语法检查，不为文档更新重复执行全套本地测试。
