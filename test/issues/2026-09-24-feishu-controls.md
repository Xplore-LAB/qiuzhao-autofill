# 小鹏飞书控件与重复经历适配

状态：1.16.9 小鹏 61 个可填写字段实页已验证；当前 1.16.10 为后续海信修复，附件与网站保存另计。保留已有值，不保存或提交。

## 已观察与复现

- 真实页面：`https://xiaopeng.jobs.feishu.cn/campus/resume/edit`。
- 1.16.6 实页预检：10 个控件，5 个规则候选；重复区块没有被发现。
- 下拉使用 `.ud__select`、`.ud__select__selector`、`.ud__select__dropdown`、`.ud__select__list__item`；已提交值在 `.ud__select__selector__selectItem`。
- 区块标题是 `.applyFormModuleWrapper-left`，同一容器内有独立“添加”按钮。作品区块没有名称输入，使用作品链接区分记录。
- `.throne-biz-date-range-picker-wrapper` 内恰好两个日期输入；实页键入 2019-09、2022-06 后，完整日期范围校验提示消失，无需翻日历。
- 获奖日期为 `.ud__picker-input[placeholder="YYYY"]`，仅接受年份。
- 学历类型、项目链接、作品描述作为独立资料字段；不把学历类型与培养方式混用。

## 回归

`test/feishu-controls-regression.cjs` 使用脱敏结构和生产脚本。修复前零行场景最终通过 0 项；第一次补齐下拉和区块后，扩展场景只能通过 28/38。补充字段、作品行锚点与年份精度后，默认零行/一行均通过 38 项，逐记录目标值一致，再运行不新增记录，不点击完成。网络全部拦截为本地夹具。

1.16.7 单元门禁 27 个脚本已通过；全量门禁与最终实页结果待本轮收尾记录。

## 首轮实页与追加修复

1.16.7 在全新小鹏编辑页运行：39.5 秒（规划 4.9、填写 29.1、核验 5.6），侧栏报告 56 已核验、43 待核对。教育 3 条、实习 2 条、项目 2 条、作品 1 条、获奖 2 条、语言 2 条均自动建立。日期范围、学历类型、项目链接、作品描述、年份和普通下拉已在页面观察到目标值。未保存或提交。

仍有 3 个学校为空，2 个城市只有搜索文字。只读 DOM 检查确认：学校使用 `.ud__select` 包裹原生输入，但没有 `.ud__select__selector`；城市搜索结果是 `.ud__tree__node`，选择标签为 `.ud__tree__node__label`。因此普通下拉策略对学校找不到触发区域，城市则找不到候选项。学校输入和城市搜索文字不按同一种控件判定。

新增最小场景保留学校的 UD 包裹结构，城市使用树形节点：修复前 36/39，通过的不是学校和城市；1.16.8 缩窄普通 UD 下拉选择器，学校走原生联想输入，城市识别树节点并点击标签。零行/一行场景修复后均 39/39，重复运行保持记录数和目标值，未点击网站完成。

模拟测试不代表城市服务器保存成功；新版实页结果另记。

## 多选城市追加修复

- 首轮 1.16.7 日志 ID：`474cad4f-ee6d-408b-8f13-46d673ad8c9b`；本地证据：`artifacts/xpeng-live/qiuzhao-run-logs-2026-09-23T18-52-24-860Z.json`（不提交）。终验日志为 56 非空、5 空白。
- 1.16.8 实页：44.5 秒，60 已核验；3 个学校和单选所在地点修复通过。期望工作地点仍未选中，手动观察确认其需要点击 `.ud__checkbox`，点击树节点文字不生效；选中值显示为 `.ud__select__selector__tag .ud__tag__content`。
- `feishu-multiselect-regression.cjs` 使用透明原生 checkbox 和可见包裹层复现；修复前 0/1，补充包裹层点击与多选标签回读后 1/1。重复运行不反选，不保存。最初夹具缺 charset 导致乱码无法映射，修正为 UTF-8 后重新取得基线，未将编码错误当控件失败证据。
- 1.16.8 完整本地门禁通过 42 个脚本；1.16.9 新增多选回归，正在进行门禁和实页复验。

## 1.16.9 小鹏实页终验

全新标签页 `2051929087`，使用 `xpeng-demo-profile.json`。自动填写 35.2 秒（规划 5.1、填写 24.6、终验 5.6），61 项核验通过；侧栏另外 38 条为资料字段/区块未在页面找到。只读 DOM 回读：可见原生非搜索输入空白 0，学校为演示高级中学/浙江大学/北京大学，已选城市为杭州/青岛；教育 3、实习 2、项目 2、作品 1、获奖 2、语言 2 条。打开开发者工具这一后续交互后值保持。工作经历保持页面原有“没有工作经历”。未保存或提交，附件另计。

1.16.9 完整本地门禁通过：43 个回归脚本与 20 项语法检查；源码指纹 `10950457ee0e102b5815d93959219a2c75832e481a8807b570d9296ffbe21b4b`。小鹏同页重复运行 7.8 秒，未新增记录；预检 61 已有、0 空白。重填报告将既有内容列为待核对，不等同新增字段失败，不能把其 0 新核验当作值被清空。

终验日志 ID：`4a8f8b0e-d469-4aab-b0af-8fd9e32f5d9a`；重复运行 ID：`d228b627-bc39-45a7-9940-89a6b54b5b74`。二者在 `artifacts/xpeng-live/qiuzhao-run-logs-2026-09-23T19-12-07-431Z.json`，对应 61 非空/0 空白，终验分别 61/0 新核验。1.16.8 首轮 ID：`e117a97f-9e38-4d80-a17e-dabf6240d16d`，60 非空/1 空白。
