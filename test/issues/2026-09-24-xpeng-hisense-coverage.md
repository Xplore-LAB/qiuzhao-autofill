# 2026-09-24 小鹏与海信实页覆盖

状态：小鹏 1.16.9 已实页核验 61 个可填写字段、0 空白；海信 1.16.9 首轮 42 项通过，1.16.10 修复已加载并启动第二轮，Mac 锁定后无法读取最终结果。附件、有效身份校验和网站保存另计。用户授权以模拟资料试填，保留已有值，不保存或提交。不能把本记录当作两站全填通过证据。

## 小鹏

- 实际 URL：`https://xiaopeng.jobs.feishu.cn/campus/resume/edit`，标题“编辑简历 - 加入小鹏集团”。
- 基本信息：姓名、邮箱、年龄、性别、国籍（地区）、所在地点、婚姻状况、期望工作地点、驾驶证。绑定手机已有值，保留。
- 教育默认 1 条，含学校、学历、学历类型、专业、起止时间；实习、项目、作品、获奖、语言默认没有记录，需要添加。
- 工作经历有“没有工作经历”复选项。不能把实习资料自动当工作经历；模拟资料明确无工作经历时才可选择。
- 简历附件另列，网站“完成”按钮不是填写操作。

## 海信

- 实际 URL：`https://jobs.hisense.com/form?fromPage=editMyResume`，页面标注 Powered by Beisen。
- 个人信息有期望面试站点、籍贯末级级联、证件、民族、户口性质、学校/学科、英语等级成绩及多种经历统计。手机已有值，保留。
- 教育、语言、项目、实习、获奖、研究成果默认各 1 组；教育要求从高中填写。
- 额外问题包括服从分配、外派、海外地区和亲属在职；仅使用明确标注模拟的答案。
- 必填证件照、简历上传及附件单独验收。无效模拟身份证不能作为身份校验成功的证据。

## 上轮交接记录（历史）

浏览器操作期间用户正在继续打开/登录其他招聘网站，控制工具连续提示窗口切换。已请求在用户完成操作后交接浏览器；实页范围仅为页面观察和小鹏预检，不记录账号手机号或整页 DOM，不声称已填好。

## 小鹏 1.16.6 阶段：预检失败与最小修复（历史）

- 1.16.5 已在新标签页加载。固定快手模拟资料预检只发现 10 个控件、0 个可靠规则候选；教育等区块均未找到添加入口。没有点击“确认填写”来制造完成数。
- DevTools 对姓名、性别节点的只读检查确认：字段使用 `.ud-formily-item`、`.ud-formily-item-label`、`data-form-field-i18n-name`；下拉外层为 `.ud__select`，内部有只读 combobox。
- `test/feishu-fields-regression.cjs` 用姓名和邮箱的脱敏最小结构加载生产脚本，修复前最终 verified=0、manual=2；补充 Formily 容器/标签及 i18n 字段名后，2 项按目标值核验通过，重复运行保留既有值，不点击“完成”。
- 此次仅实现字段标签提取。下拉、日期、经历区块尚未适配完成；`.ud__select` 的结构发现不能当作功能已支持。
- 当前源码为 1.16.6，包含标签修复；测试浏览器最后确认加载的版本为 1.16.5，不把源码修改当成已部署。

## 海信本地浏览器基准

- 新增 `test/hisense-browser-regression.cjs`，加载既有脱敏 Phoenix 行为夹具和 manifest 中的生产脚本，所有网络均拦截为本地内容。
- 3 场景各 18 项断言通过：正常搜索/选中确认/三级地区、缺失选项、确认后拒绝回写。持久化恢复为夹具内部模型模拟，不是海信服务端保存证据。
- 旧夹具消息模拟缺少当前 `LOAD_DEFAULT_PROFILE` 响应，首次调用提前退出；包装器补齐已知本地消息，未为此修改生产填写逻辑。
- 门禁自测首次因合成仓库未包含新增的三个海信指纹输入文件而失败；补齐合成空文件后，失败传播、范围标记和源码变更检测测试通过，未绕过门禁。
- `test/fixtures/xpeng-hisense-demo-profile.json` 补充高中、学科/学位、语言熟练度、获奖、研究字段与补充问题。使用无效证件号和演示值，不包含账户手机号；尚未导入浏览器，选项可用性待实页核对。

## 1.16.6 阶段未完成项与交接（历史）

1.16.6 的 `npm test` 已通过 41 个回归脚本及 20 项语法检查，报告源码指纹 `07ff6793a8d3d2da3bb47589da60c53f140dc720a1e5412c65471c57aa92fb11`。报告明确 `liveSiteVerified: false`，不替代下列实页验收。

两站字段落值、下拉确认、日期、多条经历、证件照/附件及页面校验均没有完成全量实页验收。真实账户手机保持不动；未保存或提交。

实页检查期间工具多次提示用户切换窗口，后续控制台又出现与检查命令混合的用户输入。停止浏览器交互以避免输入冲突，等待用户交接浏览器。保留原页面和新小鹏测试页，不清空已有值。

## 本轮继续（用户已交接浏览器）

已导入固定模拟资料。小鹏经历 1.16.7 首轮 56 项、1.16.8 首轮 60 项，到 1.16.9 实页 61 项全部核验、0 空白，耗时 35.226 秒；重填 7.774 秒，不新增经历、不覆盖内容。教育 3 条、实习/项目/获奖/语言各 2 条、作品 1 条。详见 [控件适配记录](2026-09-24-feishu-controls.md)。

小鹏终验日志 ID `4a8f8b0e-d469-4aab-b0af-8fd9e32f5d9a`；重复运行 ID `d228b627-bc39-45a7-9940-89a6b54b5b74`。日志的 38 条额外提示对应未发现的资料字段/区块，不是 38 个页面空白。

海信首轮 1.16.9：53.048 秒、42 核验、44 非空、62 空白，日志 ID `0872bf28-6efe-4d0a-b0c6-57d12fd20101`。延迟学校联想层未关闭，阻塞后续下拉/日期；籍贯子节点异步就绪也需等待。1.16.10 已修复并有失败基线及回归，见 [海信异步控件记录](2026-09-24-hisense-async-controls.md)。

1.16.10 在新标签页 `2051929093` 已通过 69 控件预检并启动第二轮填写；读取结果时工具报告 Mac 已锁定、无法自动解锁。已请求用户解锁，不将执行已启动当成终验成功。原页面和各轮演示页面保留。

附件上传已准备固定模拟 PDF 与带 TEST 标记的模拟头像，并单独请求上传到两站的确认；截至本记录尚未取得回复、未上传。原始日志只保存在忽略的本地 artifacts 目录，不推送。
