# 海信异步联想与地区级联

状态：1.16.10 本地修复通过，第二轮实页已启动，但最终读取受 Mac 锁屏阻塞，不能标记已验证。保留手机号及所有已有值，不保存或提交。

## 第一轮证据

1.16.9，`https://jobs.hisense.com/form?fromPage=editMyResume`，日志 ID `0872bf28-6efe-4d0a-b0c6-57d12fd20101`，53.048 秒。42 项核验通过，44 非空、62 空白。原始导出保存在忽略的 `artifacts/xpeng-live/qiuzhao-run-logs-2026-09-23T19-12-07-431Z.json`，不提交。

学校输入之后两个 `.common-unmodeled-layer` 保持可见，分别包含浙江大学和北京大学候选。其输入位于 `.phoenix-auto-complete-container`，候选是 `.phoenix-selectList__listItem`。大量后续选择项因 `choice-layer-close-blocked` 未执行。原实现输入后仅等 180ms，未等待远程候选，也未完成关闭。实页点击当前学校的 `.form-item__text` 标签后，两个联想层消失。

籍贯打开后进入地区层，实页省市节点类与已有选择器一致。日志在层出现后约 84ms 返回失败；节点卸载被当作下一级已就绪，可能早于异步子节点到达。最小测试分别延迟初始省列表和父节点卸载后的子列表，稳定复现失败。

模拟证件号被网站回退，属于预期无效模拟数据，不伪造有效身份证，不以此声称身份校验通过。单选失败仍需消除联想弹层阻塞后再判断，不提前修改单选点击逻辑。

## 修改与回归

- Phoenix 联想等待匹配候选，按本控件候选选择；若仍开着，仅点击该字段的无操作标签完成关闭，回读目标值。
- 地区每一级等待与剩余完整路径匹配的节点，不因旧父节点刚卸载就读空列表；末级和确认条件保持。
- `phoenix-autocomplete-regression.cjs`：修复前下一项学历被阻塞；修复后延迟匹配、自由输入无匹配、选中后回退三场景通过。无匹配不点击错误候选；回退不算成功。
- `phoenix-area-readiness-regression.cjs`：修复前 0/1，修复后 1/1，确认完整省市区且无残留层。
- 既有 `area-selector-regression.mjs` 全路径、缺末级、不存在末级与清理通过；1.16.10 单元门禁 27 脚本通过。

## 第二轮实页待完成

测试浏览器已在扩展详情确认 `1.16.10-dev`。新页面 `2051929093` 预检 69 控件、1 已有值、54 规则候选，按固定演示资料启动填写。随后浏览器工具返回“Mac 已锁定且无法自动解锁”，未取得终验计数与字段保持证据。需用户解锁后先读取这一轮结果，不盲目再次重填；已有证据不受影响。

附件上传确认仍待用户回复；未在海信或小鹏上传本轮准备的模拟附件。不得把未上传附件、无效模拟身份号或未保存的网站状态算作完成。

1.16.10 完整本地门禁最终通过：45 个回归脚本、20 项语法检查；源码指纹 `5f094f50b894bded2db2c4c3d24fe59d876e35356be98c4959feb4dbbbf10d40`。报告仍标记 `liveSiteVerified: false`，不替代待取得的第二轮实页结果。
