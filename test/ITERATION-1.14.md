# v1.14.0-dev：开发中，未实页验收

## 1.14.1-dev 实测后修正

- 已观察快手 Ant 下拉的零尺寸裁剪 ARIA 镜像列表；过滤其不可见子选项，防止点击辅助节点。
- Ant 下拉在 selector 触发按下/抬起/点击并聚焦输入；完整内容脚本模拟测试验证打开、所属隐藏 listbox、可见选项点击及最终回读。
- 可编辑日期输入不再当作下拉搜索文本排除，修正日期有值却报空白。
- 所有手动填写都执行最终复检，迟发回退不计成功；查看自检仍可读取最近报告。
- 实习记录增加职位名称、职责描述同义匹配，限定在已确定记录区块。
- 16 项 mjs 回归、新增 Ant 回归、普通填写延迟回退回归及海信六组基准通过。最后新增别名后的实页测试尚未进行。
- 尚未确认手机号/邮箱回退的原因，未解决所有资料分流与未知字段；未保存或提交快手简历，未更新 ZIP。需要重新加载扩展及刷新页面验证 1.14.1-dev。

## 本轮实现

| 场景 | 执行与校验 | 状态 |
|---|---|---|
| Ant 日期范围 | 分别识别起止输入；起始选择后不关闭弹层；结束后回读两端，空端/逆序/回退不计成功 | 代码与模拟回归通过，真实插件待测试 |
| 双日历导航 | 从弹层选择可见按钮，避免左面板隐藏的前进按钮 | 已读快手实际 DOM；插件端待测试 |
| 多种获奖区块 | 模型只能在页面实际区块编号之间分流；重复、无效编号及低置信度拒绝；关闭 AI 不请求 | 固定模型响应回归通过 |
| 多条记录未知字段 | 根据对应记录的资料进行模型映射，拒绝跨记录编号，不采用模型编造的值；隔离全页标量补填 | 正负回归通过 |
| 搜索经验复用 | 仅记录核验成功的搜索前缀长度与控件结构；失败删除、30 天过期、最多 100 条；不存原文或密钥 | 模拟存储回归通过 |
| 旧脚本识别 | 填写前 PING 校验 contentBuild，不一致直接提示重载，不启动填写 | 代码已接入 |

## 实页发现（2026-09-08）

- 快手竞赛记录：竞赛名称、竞赛获奖等级、竞赛获奖时间；其他荣誉仅有荣誉名称。
- 新增两条空白记录后读取结构，随后移除；未保存或提交简历。
- 教育范围只选开始日期，再 Escape：两个输入均恢复空白。证明范围不能按两个独立单日期处理。
- 双日历左面板前进按钮 visibility:hidden；右面板后退按钮同样隐藏。

## 尚未完成的验收

重新加载扩展并刷新快手后，用插件执行真实 AI 和控件填写，检查每条记录、每个选项及日期范围，再重复填充验证幂等性。保存与刷新恢复尚未测试，不将页面落值等同服务端保存。ZIP 未更新。

经验库目前复用的是受限搜索策略，并非模型训练或任意新控件驱动生成。无语义 DOM、无法确定记录边界及没有对应资料的字段仍需明确报告，不猜填。

## 回归入口

设置 QIUZHAO_JSDOM 指向已安装 jsdom；运行 test/routing-experience-regression.cjs、test/general-adaptation-regression.cjs、test/self-check-regression.cjs、test/hisense/run.cjs，以及 test/*-regression.mjs。
