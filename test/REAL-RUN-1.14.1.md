# 快手 1.14.1-dev 实页结果记录

采集日期：2026-09-08。来源：用户完成填写后，读取 Chrome 插件弹窗与表单 DOM。不是新增日志功能追溯生成的完整执行记录。

## 插件提示

- 本次验证 27，当前非空 29，当前空白 44。
- 总览时 26 个字段，规则可填 12，AI 待判断 11；总览数字早于重复记录展开，不是最终字段数。
- 教育、语言、项目、实习、研究均创建 2/2 条；此比例不是记录填写完成率。
- 三条获奖均类别未确定。
- 未完成包括教育学科/学位/培养方式/统招/海外留学、语言掌握程度、研究时间/等级/描述、招聘来源、教育/实习/项目起止时间、部分学校和专业。
- 原因提示包括控件缺失、候选无匹配或歧义、下拉未打开/旧弹层未关闭。

## DOM 核对

- 手机、邮箱当前均为空，但插件仍列在本次已填名单中。最终统计与稍后页面状态不一致，原因待继续追踪。
- 教育两组、实习两组、项目两组的开始和结束输入均为空。
- 已实际选中：最高学历、最高学历专业、两条教育学历、第二条教育专业和成绩排名、英语和日语语种。
- 与上次相比下拉选择有进展，但日期范围、资料分流和漏填未验收。
- 未保存、提交、清空或重新填写用户表单。

## 新日志入口（1.14.2-dev）

数据保存于扩展 chrome.storage.local 的 runLogs，最近 30 次，自动覆盖最旧记录。插件“运行日志”可查阅并导出 JSON，不会自动向远端发送。

记录域名（无路径/查询串）、版本、时间、耗时、AI 是否开启、汇总计数、标准字段键、序号、结果及原因。省略密钥、资料值、文档正文、完整 DOM 和自定义字段原文；陌生字段使用 unmapped 与序号。

手动正常完成、受控停止和错误均留存；发生操作的自动填充也留存，并标记 immediate（非最终复检）。无操作自动扫描不生成日志。强制刷新/关闭页面使脚本中途被销毁时，不保证产生结束日志；卸载扩展、清空插件全部数据会删除日志。

日志是运行结束时的诊断快照，不证明后续永不回退，也不证明网站已保存。Chrome 加载新代码后的实测与下载导出仍待验证。
