# 填写与修复闭环

目标是每次真实填写产生下一次可验证的改进。插件负责填写、检查和采集；开发 Agent 负责调查、复现和修复；新版本在原场景复验后才能关闭问题。插件不会自行改写或加载模型生成的代码。

## 每次填写

1. 核对浏览器加载版本，用“填写并自检”填写当前页面，保留手工已有内容。
2. 在“运行日志”查看“填写问题分析”。按站点、标准字段和原因分组；同一轮多个同名字段不增加运行次数。资料不足先补资料，配置错误先处理配置，用户停止只记未完成。
3. 有问题时导出运行日志和问题分析到本地。扩展只保留最近 30 次运行；长期问题需保留导出证据。不要把原始简历、密钥或整页 DOM 放入问题包。
4. 开发 Agent 优先使用已导出的资料与当前页面证据。如果只有脱敏元数据，不能据此断定根因；补采最小控件结构、交互顺序和演示目标值。若没有页面或必要资料，准确记录缺失证据。
5. 本轮只对明确、可逆且不覆盖已有内容的操作做有限重试；同一故障最多追加两次，不无限开关弹层或整页重填。

## 开发循环与完成条件

| 状态 | 必须具备的证据 | 下一步 |
| --- | --- | --- |
| 已观察 | runId、站点、实际加载版本、字段与失败阶段 | 判断资料、配置、控件或运行中断 |
| 已复现 | 最小夹具在修复前失败，使用固定演示资料 | 提出根因与最小修改 |
| 待实页复验 | 同一夹具在修复后通过，共享路径回归通过 | 加载新版本，在原场景复验 |
| 已验证 | 新版实页证据：目标落值、后续交互后保持、无新增误填 | 保留回归用例，记录已验证范围 |
| 待证据 | 不能复现、页面不可访问或未获必要外部操作授权 | 写清缺失证据；不算已解决 |

失败事件可能在本轮后续操作中恢复，因此问题分析优先使用最终逐字段结果；配置失败单独保留。缺少最终验证的运行不会被自动判为成功。当前分析只针对日志中的可观察问题，不证明所有控件已被发现，跨 iframe 汇总仍需另行完善。

每个实际缺陷在 `test/issues/<问题编号>.md` 留一份记录（发现具体问题时再创建），内容包括：

- 问题编号、当前状态、站点、来源 runId、浏览器/扩展版本。
- 预期与实际、最小操作顺序、证据文件路径；区分事实与根因假设。
- 最小失败测试、修复文件与变更标识、实际运行的测试及结果。
- 实页复验 runId、后续交互结果、网站保存是否另行验证。

问题分析导出的 key 是调查分组，不能用它认定不同页面的同名控件是同一个缺陷。没有复发可能只是没有再次访问该字段，不能自动关闭记录。

## 本地日志分析

在插件源码目录执行（仅读本地 JSON，不调用 AI、不发网络请求）：

```sh
node scripts/triage-run-logs.cjs /绝对路径/qiuzhao-run-logs.json
```

输出与弹窗“导出问题分析”使用同一套分类逻辑；可重定向到本地问题分析文件。输入为现有“导出运行日志”生成的带 `logs` 数组的 JSON，最大 20MB。兼容旧日志，但缺少的控件标识不能凭空补齐。

分析器回归：

```sh
node test/feedback-loop-regression.cjs
node test/run-logs-regression.cjs
```

控件修复应运行其对应测试；浏览器模拟可用 `test/hisense` 基准，但 jsdom 通过不等于 Chrome 真实扩展通过。首次接入不批量改变既有控件逻辑，不宣称历史快手/海信问题已解决。

## 当前接入状态

已接入：本地日志分类、重复问题分组、证据关联、弹窗分析与导出、离线分析命令及分析器测试。

本次已通过：分析器及 CLI 回归、日志存储回归、静态回归、日志弹窗分类与导出 DOM 测试、自检弹窗 DOM 测试。DOM 测试使用现有隔离 jsdom 依赖，没有安装或改动用户浏览器。

已在实页完成旧版日志分析弹窗查看与日志导出；第一轮已保留真实失败证据，并对日志计数、日期范围交互完成本地修复与回归，详见 [第一轮记录](test/issues/2026-09-08-first-loop.md)。现已恢复当前源码实例并完成首轮实页闭环：计数与六组日期范围通过；同时发现并修复结束日期临时预览被误跳过的问题。新日志和后续控件问题保存在第一轮记录中，验收范围限于快手演示资料场景，不代表所有站点或字段通过。没有定时后台 Agent，也不会在关闭开发会话后自动修复代码。

第二轮已完成候选失败原因贯穿执行、终验与问题分析的实页验证，详见 [候选诊断记录](test/issues/2026-09-08-choice-diagnosis.md)。资料不匹配、无候选与歧义分别给出建议；未放宽匹配来制造填写成功。
