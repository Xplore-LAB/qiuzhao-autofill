# 用户全流程与当前实现

资料管理集中到“我的简历”，按“建立资料 → 核对更新 → 填写并自检 → 网站保存 → 备份与维护”使用插件。当前源码为 `1.16.13-dev`，在 PDF 自动本地结构化解析基础上补充经历标题识别与 UD 下拉提速；本轮三站实测：快手 33.642 秒 / 80 项终验 / 1 项空白，小鹏 27.431 秒 / 61 项终验 / 0 空白，海信 29.981 秒 / 102 项终验 / 3 项空白，详见 [三站报告](docs/THREE-SITE-TEST-2026-09-24.md)。本地回归与真实招聘网站验收分开记录；有效身份校验、网站保存和正式投递未验。

## 首次使用

1. 点击扩展图标打开侧栏，再点“管理资料”进入“我的简历”管理页。首次使用无需手动创建 `content/个人资料.json`。
2. 选择适合的录入方式：
   - **手动填写**：点“填写 / 修改资料”，按基本信息、教育背景、求职意向、项目实习等分类填写，无需 AI。每次编辑自动发起保存；关闭前确认底部显示“已保存”，也可以点“立即保存”。
   - **从文件提取**：选择 PDF 简历，插件自动在本机读取并提取结构化资料，无需 API 密钥。核对原文依据、日期及多段经历，点击“应用勾选的变化”后即可填表。其他格式可点“本地解析并预览”。复杂格式可配置模型并启用 AI，再主动点“AI 提取并预览”。本地解析不发起模型请求；解析期间保持窗口打开，完成的草稿可恢复。
   - **已有插件备份**：展开“备份与恢复”，点“从备份恢复资料”，选择本插件导出的 JSON。
3. 有导入结果时，核对每项的“当前”和“更新”内容。新增项默认勾选，替换和清空默认不勾选；不确定的项取消勾选。点击“应用勾选的变化”后才写入资料。
4. 首页提示姓名、手机、邮箱是否缺失。它只检查常用信息是否存在，不代表资料完全正确，也不要求填写每个字段。
5. 返回招聘页面，从浏览器工具栏重新打开插件；如有多个表单区域，先选择本次区域。点击“识别并检查表单”，核对后点击“确认填写并自检”。大窗口用于管理资料，不直接填写招聘页面。

侧栏会同步已保存的资料和 AI 开关。姓名、手机号或邮箱至少有一项非空时可启动填写；清空这些信息后，侧栏会提示先建立资料。点击填写时还会重新读取当前设置，避免使用过期的 AI 开关。

可选的 `content/个人资料.json` 仅在内容变化时自动导入，并保存导入前的恢复记录。文件未变化时不会覆盖管理页中的修改；缺少或删除文件时继续使用浏览器本地资料。已有文件格式错误时先修正文件再填写。

## 平时投递

| 阶段 | 用户操作 | 插件行为 |
| --- | --- | --- |
| 开始前 | 核对本次投递要用的资料 | 使用当前已保存资料；待核对的导入结果不会用于填表 |
| 填写 | 检查表单后点“确认填写并自检” | 仅填写所选区域，保留页面非空字段；页面或资料变化后需重新检查 |
| 必要覆盖 | 主动勾选“本次覆盖非空字段”再填写 | 仅用于该轮，发起填写后取消勾选；右键、快捷键始终不覆盖 |
| 中途停止 | 点“停止填写” | 停止后续任务，报告已有进度；不会自动撤销网站中已经填写的值 |
| 结果核对 | 查看成功、待核对、失败字段 | 自检报告区分页面落值和网站保存，保留可定位的待处理项 |
| 完成投递 | 补充剩余项，上传附件简历，在网站自行保存或提交 | 不自动点击网站保存、暂存或提交按钮 |

自动填写开关仍是可选项，默认关闭；复杂下拉和日期推荐使用手动“填写并自检”。“快速填充”、右键与快捷键保留为快捷入口，首次使用优先走主流程。

## 更新自己的简历和相关信息

**少量修改**：在相应资料分类直接改手机号、邮箱、求职意向或经历，等“已保存”即可。已打开网站里的旧值不会随之自动刷新；需要在网站核对并修改。

**新版本简历**：回到“我的简历”选择新 PDF → 自动本地解析并预览 → 逐项核对 → 应用。新文件没提到的旧资料会保留；不根据缺失内容推断删除。教育、项目、实习等多条经历按整组比较和替换，应核对整组，避免只保留新文件中的最后一条经历。

**误用更新**：展开“备份与恢复”，点“查看上次更新前的资料”。恢复也先展示差异，勾选希望还原的项后应用。只保留最近一次批量应用前的快照；手动编辑没有逐次撤销历史。恢复操作本身也会保留其应用前的资料快照。

**暂时不处理更新**：可关闭窗口，已完成的更新草稿会保存在本地；重新打开后仍能核对。当前勾选状态不跨窗口保存，重新打开后按默认规则重新选择。要导入另一份资料，先应用或放弃现有更新。

## 升级、重装和换电脑

1. 在旧环境点“导出资料备份”，保管好 JSON。它含个人资料，还保留旧格式中的设置、学习记录和站点规则；不含 API 密钥、原始简历文本、待核对草稿或恢复快照。
2. 原目录升级：更新源文件后重新加载同一个扩展，再刷新招聘页面。不要通过卸载来升级。先核对资料是否存在，再开始投递。
3. 重装、换浏览器配置或换电脑：安装后点“从备份恢复资料”，核对并应用所需资料。此入口只恢复个人资料，不恢复备份中的设置、学习记录和站点规则。
4. 需要 AI 时重新配置厂商和密钥；需要继续从原始简历提取时重新选择源文件。资料只保存在当前浏览器，不自动跨设备同步。

## 异常时用户怎么做

自检面板现在按“填写失败、补充资料、空白待判断、已有内容待核对、重新核对、已验证”分别列出数量。每项下方直接显示原因和下一步，点击字段可回到网页定位。“空白待判断”不能直接当成缺资料；“已有内容待核对”只需检查网页已有值。新版脱敏导出保留操作分类和建议，已识别字段可携带标准编号，仍不包含个人资料值。实际验证范围见 [自检提示修复记录](test/issues/2026-09-10-self-check-guidance.md)。

| 提示或情况 | 可执行的下一步 |
| --- | --- |
| 没有资料 | 点“填写 / 修改资料”或选择简历文件；未建立资料时不发起整页填写 |
| 文件读取失败 | 改用支持的格式或缩小文件，当前已保存资料不变 |
| AI 未配置、失败或超时 | 检查 AI 配置后重试，也可直接手动填写；失败结果不覆盖资料 |
| 更新预览出现冲突 | 最新资料已用于重新比较，重新勾选要应用的项 |
| 手动编辑出现并发冲突 | 点“核对并发修改”，比较本页未保存内容与最新资料；如已有导入预览，先放弃该预览再处理 |
| 保存失败 | 保持窗口打开，点“立即保存”重试；没有“已保存”不能视为写入成功 |
| 页面脚本版本不一致 | 重新加载扩展，再刷新招聘页面后重试 |
| 资料缺失导致未填 | 先补资料，再填写相应空项 |
| 候选缺失、歧义或控件失败 | 在运行日志查看原因，导出脱敏诊断进入开发闭环，不重复整页覆盖 |

## 稳定性实现与边界

- 资料写入通过扩展后台串行处理，并对实际改变的字段检查预期旧值：不同字段的并发修改合并，同字段变化提示核对。
- 批量更新的资料、恢复快照和草稿清理在一次存储写入中完成；写入失败不显示应用成功。手动保存失败不会阻断队列后续重试。
- 保留原有自定义下拉值；未改动的多条经历数组保持结构。输入过程中不重写用户正在编辑的文本，避免空格消失或光标跳动。
- 导入校验对象结构、字段白名单、体积和不支持的键，预览只渲染文本。来源材料不作为代码执行。
- 支持 `.pdf`、`.docx`、`.xlsx`、`.xls`、`.csv`、`.json`、`.txt`、`.md`；文件上限 20MB、读取文本上限 30 万字符。PDF 限 50 页、30 秒，本地解析中文/英文文本，扫描件或加密文件会提示处理，不会发送到外网。备份 JSON 上限 5MB、资料对象上限 1MB。目前不支持扫描件 OCR、多份简历切换和云同步。
- 当前保留一份资料和一份待核对更新；建议集中在一个大窗口做批量更新。同字段冲突保护针对个人资料，不代表 AI 设置、站点规则和原始文件都支持并发合并。
- 不改变既有填写闭环的证据标准：本地测试通过不代表所有网站兼容。发生问题按 [FEEDBACK-LOOP.md](FEEDBACK-LOOP.md) 的“观察 → 复现 → 修复 → 回归 → 实页复验”处理。

## 资料管理功能的历史验证

新增回归验证差异默认选择、局部合并、存储失败重试、恢复记录、跨窗口冲突、预览重开、输入空格、结构化经历、文件先本地读取、AI 结果先预览、备份不改变 AI 设置；后台测试覆盖资料写入入口和快捷方式不覆盖。

既有静态、AI、填充停止、控件、重复经历、自检和日志回归已运行。旧日期范围测试补齐了生产函数新增的测试依赖。

浏览器测试页使用真实弹窗 HTML/CSS/JS 和模拟扩展接口，固定演示资料验证了：首次建档、自动保存、84 项差异预览、只合并新增项、保留未勾选原值、82 项清空恢复预览默认不选、重新打开保留草稿、420px 弹窗无横向溢出及大窗口布局。

该阶段浏览器控制入口无法连接 Google Chrome，故当时没有标记为招聘实页验收。后续已恢复连接并完成快手、小鹏演示填写；最新逐站状态见下文及覆盖记录。

## 日期与多条经历

在资料管理页分别填写教育、实习、项目和语言等记录。IT 技能、技能证书、竞赛获奖、其他荣誉、作品信息使用独立数组，避免同名区块串填。识别预览会展示资料条数和页面已有行数；确认后先补足缺少的空行，再按记录填写。已有内容仍保留，重复运行不以清空或覆盖来制造成功。

只读 Ant 日历优先打开年份和月份面板，再选目标日；可编辑日期按控件粒度输入。只有年月的资料在快手日期控件中使用当月 01 日。禁用日期和不前进的面板会停止该项并报告，不无限翻页。

逐站验收与后续适配排期见 [招聘网站与典型表单覆盖清单](docs/WEBSITE-COVERAGE.md)。

1.16.3 的 Ant 下拉和日历在控件就绪后继续，等待菜单真正关闭后才操作下一项。页面较慢时仍等待或报告超时，不省略最终核验。提速效果以 [实页验证记录](test/issues/2026-09-24-autofill-speed.md) 为准。

1.16.4 合并普通文本写入与失焦等待，仍在下一项开始前检查节点、值和原生校验；不覆盖联动后已有的值。快手本轮总耗时约 33 秒，详见 [文本提速验证](test/issues/2026-09-24-native-wait-speed.md)。

小鹏 1.16.9 实页自动填写 61 项，35.2 秒、0 空白；重复运行保留内容且不新增经历。附件另计。海信 1.16.10 已加载并启动第二轮，读取最终结果时 Mac 锁定，尚不能认定实页全部通过。实页进展及剩余项见 [覆盖记录](test/issues/2026-09-24-xpeng-hisense-coverage.md)。
